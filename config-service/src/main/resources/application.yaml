server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: gateway
          uri: https://github.com/Luisiigna/microservicios-practica-intento
          search-paths: config-data
  application:
    name: config-server